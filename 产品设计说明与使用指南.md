# CardFlow（卡片智刷）— 产品设计与使用指南

> 版本：v1.0.0 ｜ 最后更新：2026-08-27
> 适用代码：`cardflow/` 工程（Cloudflare Workers + KV + Vue3/Swiper 纯前端）
> 配套文档：`overview.md`（UI/UX 落地记录）、`deploy_guide.md`（Cloudflare 部署步骤）

---

## 目录

1. [产品定位与设计哲学](#一产品定位与设计哲学)
2. [功能全景](#二功能全景)
3. [核心交互模型](#三核心交互模型)
4. [卡片题型（Layout）设计规格](#四卡片题型layout设计规格)
5. [视觉设计系统](#五视觉设计系统)
6. [系统架构](#六系统架构)
7. [内容生产工作流（card_tool.js）](#七内容生产工作流card_tooljs)
8. [使用说明](#八使用说明)
9. [目录结构与文件清单](#九目录结构与文件清单)
10. [已知边界与后续路线](#十已知边界与后续路线)

---

## 一、产品定位与设计哲学

### 1.1 产品是什么
CardFlow 是一个**抖音式（TikTok 式）知识流刷卡 Web 应用**：把知识内容拆成一张张可滑动的卡片，用户像刷短视频一样上下滑主题、左右滑子卡，在碎片时间里完成轻量、低阻力的学习。支持知识卡（农业等权威知识）与放松卡（笑话、梗图、小游戏）按可配置比例穿插，兼顾「学」与「歇」。

### 1.2 解决什么问题
- **传统学习产品阻力大**：长文、目录、跳转打断心流。CardFlow 把内容切片成「一屏一卡」，一滑即过。
- **内容可信度存疑**：每张知识卡都带**权威来源抽屉**（机构、文档、发布日期、可信级别），可追溯、零幻觉。
- **持续学习易疲劳**：通过 `relax_ratio` 在密集知识流里自动插入放松卡，避免认知过载。

### 1.3 四大设计哲学
| 哲学 | 含义 | 落地方式 |
|------|------|----------|
| **抖音式低阻力交互** | 一屏一卡、滑动即学、无需菜单 | 上下滑主题 + 左右滑子卡；翻牌/笑话/小游戏即点即玩 |
| **可变比例放松机制** | 学习密度可调，避免疲劳 | `global_config.relax_ratio`（默认 5：每 5 张知识卡插 1 张放松卡） |
| **零幻觉可追溯** | 知识必有出处，不编造 | 每张知识卡带 `source`，底部「来源」角标开抽屉；`type=knowledge` 缺 source 会在校验阶段被拦截 |
| **极致无服务器** | 全球边缘毫秒访问、零运维 | Cloudflare Workers + KV 边缘读取；前端纯静态、无需构建步骤 |

### 1.4 目标用户与使用场景
- **终端用户**：利用通勤、排队等碎片时间刷知识 + 解压的普通用户；移动端为主。
- **内容运营 / 编辑**：用 JSON 维护题库的运营人员（本指南第七章、第八章为其服务）。
- **部署者**：拥有 Cloudflare 账号、负责上线的开发者（见 `deploy_guide.md`）。

---

## 二、功能全景

| 模块 | 功能 | 说明 |
|------|------|------|
| 内容流 | 纵向主题流 | 每个 topic 一屏；上滑看下一个、下滑看上一个 |
| 内容流 | 横向子卡 | 一个 topic 内含多张子卡（问答、流式、翻牌…），左右滑动切换 |
| 题型 | 6 种 layout | qa_card / streaming_text / flip_card / joke_text / meme_card / game_card |
| 交互 | 打字机流式 | `streaming_text` 进入视口逐字显示，已播放过不重复 |
| 交互 | 3D 翻牌 | `flip_card` 点击翻转看答案，**独占满屏** |
| 交互 | 笑话气泡 + 揭晓 | `joke_text` 微信风格气泡，点击「揭晓笑点」 |
| 交互 | 梗图 / 小游戏 | `meme_card`（大图+配文，独占满屏）、`game_card`（石头剪刀布等） |
| 可信 | 引用抽屉 | 底部「来源」角标 → 半屏抽屉展示权威出处 |
| 个人 | 收藏 | 🔖 收藏写入 `localStorage['bookmarked_cards']`，刷新保留 |
| 个人 | 朗读 | 🔊 Web Speech API（`zh-CN`）朗读正文，再点停止；翻牌背面也支持 |
| 个人 | 已读 | 看过即记 `localStorage['read_topics']`（去重） |
| 系统 | 版本感知 | 顶部「发现新库 ↺」；`min_app_version` 高于客户端则强制提示更新 |
| 系统 | 放松混排 | 按 `relax_ratio` 自动穿插放松卡 |
| 工具 | 内容校验/生成 | `tools/card_tool.js`（零依赖 CLI，详见第七章） |

---

## 三、核心交互模型

### 3.1 双向滑动（Swiper 嵌套）
- **纵向（`.vertical-swiper`，`direction:'vertical'`）**：切换**主题（topic）**。每次切换触发该主题子卡的流式播放与已读记录。
- **横向（`.horizontal-swiper`，`direction:'horizontal'`，`nested:true`）**：在同一主题内切换**子卡（sub_card）**。
- 内嵌分页点（`.swiper-pagination`，翡翠绿 `#10B981` 高亮）指示当前子卡位置。

### 3.2 翻牌 / 梗图独占满屏
`flip_card` 与 `meme_card` 进入 `fillMode`：外层 `slide` 去除内边距（`p-0`），内容区 `overflow-hidden`，卡片**铺满整屏**且 3D 旋转期间不再出现上下/左右拖动条。
> 实现要点（已踩坑修复）：3D 翻转容器不能挂 `backdrop-filter` / 非 `visible` 的 `overflow`，否则 `transform-style: preserve-3d` 被强制降级为 `flat`，翻转退化为 2D 镜像。玻璃背景只放在正反面叶子元素上，容器保持纯净。

### 3.3 打字机流式
`streaming_text` 子卡进入视口时，以 ~30ms/字 逐字渲染 `streaming_content`；用 `store.streamingDisplay[key]` 缓存，**已播放过不重复触发**。

### 3.4 放松卡
- `joke_text`：微信风格对话气泡（铺垫），点击「揭晓笑点」展开 punchline。
- `meme_card`：整屏大图 + 配文（`caption`）。
- `game_card`：纯事件驱动的小游戏（如石头剪刀布），无定时循环。

### 3.5 引用抽屉
知识卡底部「来源」角标点击 → 通过 `<Teleport to="body">` 渲染的半屏抽屉（纯 CSS `z-index:9999` 实底遮罩），展示 `source`：机构名、文档标题、URL、发布日期、可信级别（`authority_level`）。
> 实现要点：`bg-black/70`、`z-[9999]` 等 Tailwind 任意值/opacity 修饰符在预编译版不生成 → 一律用纯 CSS 类 `.source-overlay` / `.source-panel` / `.source-close`。

### 3.6 收藏 / 朗读 / 已读（localStorage）
| 能力 | 触发 | 存储键 |
|------|------|--------|
| 收藏 | 底部 🔖 | `bookmarked_cards`（对象：`sub_id -> true`） |
| 已读 | 滑过主题自动 | `read_topics`（数组去重） |
| 朗读 | 底部 🔊 | 内存态 `store.speaking`，不持久化 |
| 版本 | 拉取索引时比对 | `cardflow_last_version` |

### 3.7 版本感知
- 客户端常量 `CLIENT_APP_VERSION = '1.0.0'`。
- 拉取 `app:index` 后：用语义化比较 `isVersionGt` 比对 `indexData.version` 与本地 `cardflow_last_version` → 顶部显示「发现新库 ↺」；若 `indexData.min_app_version` 高于客户端版本 → 强制 `store.hasUpdate = true`。
- 写入回 `cardflow_last_version`，下次启动据此判断。

---

## 四、卡片题型（Layout）设计规格

### 4.1 总览
| layout | 用途 | 独占满屏 | 专属必填字段 |
|--------|------|----------|--------------|
| `qa_card` | 问答卡（左右滑子卡之一） | 否 | `content` |
| `streaming_text` | 打字机流式卡 | 否 | `streaming_content` |
| `flip_card` | 3D 翻牌（点翻转） | **是** | `front_text`、`back_text` |
| `joke_text` | 笑话气泡卡 | 否 | `content`、`punchline` |
| `meme_card` | 梗图卡（大图+配文） | **是** | `image`、`caption` |
| `game_card` | 小游戏卡 | 否 | （仅 `title`） |

### 4.2 通用字段（所有 topic / sub_card）
- **topic（item）**：`id`、`title`、`tags[]`、`type`（`knowledge`/`joke`）、`sub_cards[]`；`type=knowledge` 必须带 `source`。
- **sub_card**：`sub_id`、`layout`、`title`；各 layout 额外字段见上表。

### 4.3 各题型填写要点
- **qa_card**：`content` 为正文，`action_hint` 引导继续滑动，`citation_anchor` 写引用锚点（如「第一章第2条」）。
- **streaming_text**：`streaming_content` 为逐字展示文本；长文拆成多个子卡更友好。
- **flip_card**：`front_text` 设疑、`back_text` 给答案；`citation_anchor` 指向出处。
- **joke_text**：`content` 铺垫、`punchline` 笑点（揭晓后展开）。
- **meme_card**：`image` 用稳定可访问的图片 URL（如 `https://picsum.photos/seed/xxx/600/600`），`caption` 配文。
- **game_card**：目前由前端内置交互驱动，子卡只需 `title`。

---

## 五、视觉设计系统

### 5.1 设计语言
- **玻璃拟态（Glassmorphism）**：卡片 `.glass-card`；放松卡 `.glass-card-fun` 暖色发光边框。
- **氛围光晕（Ambient Glow）**：背景 `.ambient` 光晕随类型切换——知识卡**绿蓝**、放松卡**橙**（`glowClass` 计算属性驱动）。
- **字号阶梯**：标题 `card-title` ≈21–22px/700，副标题 ≈18px/600，正文 `card-body` ≈17px/行高 1.75，页脚 `card-footer-text` 14px。
- **头图区**：140px（`stream-min` 最小高度），按分类动态图标/渐变；`hero-knowledge` / `hero-fun` 两套样式。
- **分页点**：内嵌卡下，翡翠绿 `#10B981` 高亮当前子卡。

### 5.2 ⚠️ Tailwind 预编译约束（必须遵循）
本项目 `public/index.html` 引入的是**预编译版 Tailwind**（`tailwindcss@2.2.19/dist/tailwind.min.css`），**不是 JIT Play CDN**。以下写法**不生效**，一律改用 `style.css` 里的纯 CSS 类：
- 带 opacity 修饰符的颜色类：`bg-black/70`、`bg-slate-800/95`、`border-emerald-800/60` …
- 任意值类：`h-[80vh]`、`min-h-[140px]`、`z-[9999]` …
- 不存在的色阶：`emerald-950` 等 v2.2 调色板没有的 950 档。

> 已落地的纯 CSS 类（在 `style.css`）：`.card-shell`(80vh)、`.stream-min`(140px)、`.source-overlay/panel/close`、`.ambient`/`.glow-*`、`.glass-card`/`.glass-card-fun`、`.badge-knowledge`/`.badge-fun`、`.flip-back`、`.divider`、`.card-title/body/subtitle/footer-text`、`.card-hero`/`.hero-*`、`.chat`/`.bubble*`/`.reveal-btn`、`.action-btn`、分页点样式。

---

## 六、系统架构

### 6.1 总体架构
```
┌──────────── 浏览器（纯静态前端） ────────────┐
│  Vue3 (CDN, 无构建) + Swiper                │
│  四层组件：数据 → CardShell → Layout 分发 →   │
│  Foundation(CSS)                            │
└───────────────┬─────────────────────────────┘
                │  fetch /api/index , /api/bundle?id=
┌───────────────▼─────────────────────────────┐
│  Cloudflare Workers（边缘）                  │
│  读 KV：app:index / bundle:*                 │
│  分发静态资源                                │
└───────────────┬─────────────────────────────┘
                │  KV 读取
        ┌───────▼────────┐
        │  CARD_KV 命名空间│
        │  app:index      │
        │  bundle:agri:v1 │
        │  bundle:fun:v1  │
        └─────────────────┘
```
**本地开发**无需账号：`local-server.js`（纯 Node，端口 8788）模拟 KV，直接读 `data/*.json`。

### 6.2 前端：四层组件架构
1. **数据层**：`fetchAllTopics()` 拉取索引与各 bundle，合并为 `topics`；`mixTopics(topics, ratio)` 做 relax 混排 + 去重。
2. **CardShell（外壳）**：统一 80vh 卡片、头图、徽章+阅读时长、内容插槽、底部 footer；`fillMode` 控制独占满屏。
3. **Layout 分发**：`layoutMap` 把 `sub.layout` 映射到组件，`<component :is="layoutComp(sub.layout)">` 数据驱动渲染。新增题型**零改框架**——只加一个 layout 组件 + 在 `layoutMap` 注册。
4. **Foundation**：`style.css` 纯 CSS 类 + 玻璃拟态/光晕/字号体系。

状态用 `reactive` 单例 `store` + `services`（openSource / toggleBookmark / toggleSpeak）经 `provide/inject` 注入。

### 6.3 后端：Workers + KV
- `worker/index.js`：`/api/index` 返回 `app:index`；`/api/bundle?id=` 返回对应 bundle；其余路径分发静态资源。
- 数据全在 KV，边缘毫秒级；前端纯静态，改内容只需重灌 KV，无需改后端。

### 6.4 本地预览
```bash
cd cardflow
node local-server.js          # 打开 http://localhost:8788
```

### 6.5 数据契约
**`data/app_index.json`**（索引）
```json
{
  "version": "2026.08.23.01",
  "min_app_version": "1.0.0",
  "global_config": { "relax_ratio": 5, "notice": "..." },
  "categories": [
    { "id": "agriculture", "name": "农业知识", "description": "...",
      "total_bundles": 1, "bundles": ["bundle:agriculture:v1"] },
    { "id": "entertainment", "name": "放松时刻", "description": "...",
      "total_bundles": 1, "bundles": ["bundle:entertainment:v1"] }
  ]
}
```

**`data/bundle_*.json`**（题库包）
```json
{
  "bundle_id": "bundle:agriculture:v1",
  "category_id": "agriculture",
  "version": "1.0.1",
  "items": [
    {
      "id": "agri_topic_001", "title": "...", "tags": ["病虫害","小麦"],
      "type": "knowledge",
      "source": { "name": "...", "title": "...", "url": "https://...",
                  "publish_date": "2026-01-15", "authority_level": "official" },
      "sub_cards": [
        { "sub_id": "s1", "layout": "qa_card", "title": "...", "content": "...",
          "action_hint": "左右滑动查看防治三步法 ➔", "citation_anchor": "第一章第2条" },
        { "sub_id": "s2", "layout": "streaming_text", "title": "...",
          "streaming_content": "...", "citation_anchor": "第一章第3条" },
        { "sub_id": "s3", "layout": "flip_card", "title": "...",
          "front_text": "...", "back_text": "...", "citation_anchor": "第二章第1条" }
      ]
    }
  ]
}
```

### 6.6 关键算法
- **relax_ratio 混排**：学习卡（`type!=='joke'`）与放松卡分开，每间隔 `ratio` 张学习卡插入 1 张放松卡；放松卡不足则排在末尾。`ratio` 取自 `global_config.relax_ratio`。
- **语义化版本比较** `isVersionGt(a,b)`：按 `.` 分段数值比较，用于版本感知。

---

## 七、内容生产工作流（card_tool.js）

零依赖 Node CLI（`tools/card_tool.js`），与 `app.js` 的 `layoutMap` 口径一致。用途：部署前把关 JSON、加卡时自动补齐字段。

### 7.1 校验 `validate`
```bash
node tools/card_tool.js validate                 # 校验 data/ 全量 + 跨文件核对
node tools/card_tool.js validate data/bundle_x.json   # 校验单个文件
# 或： npm run validate
```
校验项（曾踩过的坑全覆盖）：
- 合法 JSON（**全角引号做定界符会崩**，并提示「换成半角 `"`」）；
- 必填字段、`layout` 枚举（6 种）、各 layout 专属必填；
- `type=knowledge` 必须带 `source`；
- `id` / `sub_id` 重复；
- `citation_anchor` 含全角逗号（手误预警）；
- `relax_ratio` / `min_app_version` 缺失警告；
- 跨文件：索引声明的 bundle 文件是否存在、分类 id 是否重复。
- **退出码 1** 表示有错误，可直接接部署前检查。

### 7.2 生成 `new`
```bash
# 新建一个 topic（写入新 bundle 文件）
node tools/card_tool.js new topic "玉米锈病识别" \
     --type knowledge --layouts qa_card,flip_card \
     --out data/bundle_maize.json

# 追加一个 topic 到已有 bundle（自动查重）
node tools/card_tool.js new topic "小麦白粉病" \
     --type knowledge --layouts streaming_text \
     --append data/bundle_agri.json

# 仅打印一张子卡 JSON（便于手动粘贴）
node tools/card_tool.js new subcard meme_card --title "程序员的浪漫"

# 生成空 bundle 骨架
node tools/card_tool.js new bundle bundle:cat:v1 --category agriculture
```
生成时会按 layout 自动补齐该 layout 的专属字段（含知识卡 `source` 模板），并用 `TODO:` 占位，提醒替换。

### 7.3 字段填写规范（重点）
- 所有中文文本**用英文半角 `"` 作 JSON 定界符**（全角引号会直接崩解析）。
- `citation_anchor` 用半角逗号或文字描述，避免全角逗号。
- 知识卡 `source.url` 以 `http(s)://` 开头，`authority_level` 如 `official`。
- 每张子卡 `sub_id` 在同一 topic 内唯一；每个 topic `id` 全局唯一。
- 完成后务必跑 `npm run validate` 再过部署。

---

## 八、使用说明

### 8.1 终端用户使用说明
1. 打开应用（本地 `http://localhost:8788` 或部署后的域名）。
2. **刷知识**：上下滑切换主题，左右滑查看同一主题下的子卡（问答 / 流式 / 翻牌）。
3. **翻牌**：遇到翻牌卡，点击卡片翻转看答案（整屏翻转，无干扰）。
4. **解压**：放松卡会自动穿插——笑话点「揭晓笑点」，梗图整屏看，小游戏直接玩。
5. **看来源**：卡片底部「来源」角标 → 点开抽屉看权威出处。
6. **收藏**：点 🔖 收藏，刷新后仍在（存浏览器本地）。
7. **朗读**：点 🔊 听正文（中文），再点一次停止。
8. **更新提示**：顶部出现「发现新库 ↺」说明内容已更新，按提示刷新即可。

### 8.2 内容编辑 / 运营使用说明（加一张新卡的标准流程）
1. **生成脚手架**：`node tools/card_tool.js new topic "标题" --type knowledge --layouts qa_card,flip_card --append data/bundle_agri.json`（或 `--out` 新建 bundle）。
2. **填写内容**：打开生成的 JSON，把 `TODO:` 占位替换为真实内容，补全 `source` 出处。
3. **（新 bundle 时）登记索引**：在 `data/app_index.json` 对应分类的 `bundles` 数组里加上新 `bundle_id`，并同步 `total_bundles`。
4. **校验**：`npm run validate`，确保 0 错误。
5. **本地预览**：`node local-server.js` 打开 `http://localhost:8788` 实际刷一遍，确认滑动/翻牌/抽屉正常。
6. **上线**：按 `deploy_guide.md` 把新 bundle 灌入 KV 并部署（见 8.3）。

### 8.3 部署说明（要点，完整步骤见 `deploy_guide.md`）
- 需本人 `wrangler login` + 建 KV 命名空间 `CARD_KV` + 把 `id` 填入 `wrangler.toml`。
- 灌库：`wrangler kv:key put --binding=CARD_KV "app:index" --path="./data/app_index.json"`（及每个 bundle）。
- 部署：`wrangler deploy`。**注意** `wrangler.toml` 的 `compatibility_date` 应设为当天日期。
- 前端纯静态、无构建；改内容只重灌 KV，不动后端。

---

## 九、目录结构与文件清单

```
cardflow/
├── public/
│   ├── index.html      # 入口（预编译 Tailwind + Vue3/Swiper CDN）
│   ├── app.js          # 四层架构核心：store/services、mixTopics、Swiper、组件
│   └── style.css       # 纯 CSS 设计系统（玻璃拟态/光晕/字号/抽屉…）
├── data/
│   ├── app_index.json  # 索引（版本 / relax_ratio / 分类）
│   ├── bundle_agri.json    # 农业知识包
│   └── bundle_fun.json     # 放松包（笑话/梗图/小游戏）
├── worker/
│   └── index.js        # Cloudflare Workers 边缘读取 KV + 分发静态
├── tools/
│   └── card_tool.js    # 内容校验/生成 CLI（零依赖）
├── local-server.js     # 本地预览（模拟 KV，端口 8788）
├── wrangler.toml       # Workers 配置
├── package.json        # scripts: dev/deploy/preview/validate/new
├── overview.md         # UI/UX 落地记录
├── deploy_guide.md     # Cloudflare 部署步骤
└── 产品设计说明与使用指南.md  # 本文档
```

---

## 十、已知边界与后续路线

**已知边界（P0 前）**
- 内容量偏少（农业 1 topic、放松 3 topic），`relax_ratio` 穿插、去重、版本提示的实际价值尚未充分显现——**下一步 P0 即内容铺量**。
- 收藏目前仅 `localStorage` 本地态，无跨设备同步（如需可后续接 KV）。
- 移动端真机/DevTools 实测验收待做。

**后续路线（建议优先级）**
1. **P0 内容铺量**：用 `card_tool.js` 批量补真实农业卡片 + 更多放松卡。
2. 收藏夹画廊 + 已读进度 X/Y（让 🔖 与已读可见）。
3. 移动端真机验收。
4. KV 真部署（用户确认延后，见 `deploy_guide.md`）。

---

*本文档与 `overview.md`、`deploy_guide.md` 互为补充：本文件负责「设计 + 使用总纲」，另两份分别聚焦「UI 落地记录」与「部署步骤」.*
